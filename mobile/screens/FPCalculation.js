const FPCalculation = ({ route }) => {
    // firebase new collection to track which method is bieng used
    // user story me weiht assign krty waqat hi input output wagira puch lo agar fp metrice use ho raha h to
    

    return (
        <View style={
            {
                justifyContent: 'center',
                alignItems: 'center',
                flex: 1,
            }
        }>
        </View>
    );
}

export default FPCalculation;